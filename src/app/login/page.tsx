"use client";
import Link from "next/link";import {useRouter} from "next/navigation";import {Building2} from "@/components/icons";import {DemoRole,useDemo} from "@/components/demo-provider";
export default function Login(){const {signIn}=useDemo();const router=useRouter();return <main className="grid min-h-screen place-items-center bg-[#173c34] p-5"><section className="w-full max-w-md rounded-3xl bg-white p-7 shadow-2xl sm:p-10"><Link href="/" className="flex items-center gap-2 font-serif text-xl font-bold text-[#173c34]"><Building2/>Betoch</Link><h1 className="mt-8 font-serif text-3xl font-semibold text-[#173c34]">Welcome back</h1><p className="mt-2 text-sm text-stone-500">Sign in to save homes, contact sellers, or manage listings.</p><form className="mt-7 space-y-4" onSubmit={e=>{e.preventDefault();const data=new FormData(e.currentTarget);const role=String(data.get("role")) as DemoRole;signIn(String(data.get("email")),role);router.push(role==="admin"?"/admin":role==="buyer"?"/favorites":"/dashboard")}}><label className="block text-sm font-semibold">Email<input required name="email" type="email" className="mt-2 h-12 w-full rounded-xl border px-4 font-normal" defaultValue="demo@betoch.et"/></label><label className="block text-sm font-semibold">Password<input required name="password" type="password" minLength={8} className="mt-2 h-12 w-full rounded-xl border px-4 font-normal" defaultValue="betoch-demo"/></label><div className="text-right"><Link href="/forgot-password" className="text-xs font-bold text-[#173c34]">Forgot password?</Link></div><label className="block text-sm font-semibold">Demo role<select name="role" className="mt-2 h-12 w-full rounded-xl border px-4 font-normal"><option value="buyer">Buyer / renter</option><option value="owner">Property owner</option><option value="broker">Broker</option><option value="admin">Administrator</option></select></label><button className="h-12 w-full rounded-xl bg-[#d85d3f] font-bold text-white">Continue</button></form><p className="mt-6 text-center text-sm text-stone-500">New to Betoch? <Link href="/register" className="font-bold text-[#d85d3f]">Create an account</Link></p><p className="mt-6 rounded-xl bg-amber-50 p-3 text-xs leading-5 text-amber-900">Demo mode stores actions in this browser. Production authentication switches to Supabase when configured.</p></section></main>}
