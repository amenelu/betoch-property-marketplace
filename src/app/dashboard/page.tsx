"use client";import Link from "next/link";import {DashboardShell} from "@/components/dashboard-shell";import {useDemo} from "@/components/demo-provider";import {Home} from "@/components/icons";
export default function Dashboard(){const {ready,user}=useDemo();if(!ready)return <main className="grid min-h-screen place-items-center">Loading workspace…</main>;if(!user||!(["owner","broker","admin"] as string[]).includes(user.role))return <main className="grid min-h-screen place-items-center bg-[#173c34] p-5"><section className="max-w-md rounded-3xl bg-white p-8 text-center"><Home className="mx-auto text-[#287864]" size={38}/><h1 className="mt-4 font-serif text-3xl text-[#173c34]">Seller access required</h1><p className="mt-2 text-sm leading-6 text-stone-500">Create an owner or broker account to publish properties and manage inquiries.</p><div className="mt-6 flex justify-center gap-3"><Link href="/register" className="rounded-full border px-5 py-3 font-bold">Create account</Link><Link href="/login" className="rounded-full bg-[#d85d3f] px-5 py-3 font-bold text-white">Sign in</Link></div></section></main>;return <DashboardShell/>}
