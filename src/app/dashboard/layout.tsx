"use client";import Link from "next/link";import {useDemo} from "@/components/demo-provider";
export default function SellerLayout({children}:{children:React.ReactNode}){const {ready,user}=useDemo();if(!ready)return <main className="grid min-h-screen place-items-center">Loading…</main>;if(!user||!(["owner","broker","admin"] as string[]).includes(user.role))return <main className="grid min-h-screen place-items-center bg-[#173c34] p-5"><section className="max-w-md rounded-3xl bg-white p-8 text-center"><h1 className="font-serif text-3xl text-[#173c34]">Seller access required</h1><p className="mt-2 text-sm text-stone-500">Sign in as an owner or broker to manage property listings.</p><Link href="/login" className="mt-6 inline-flex rounded-full bg-[#d85d3f] px-6 py-3 font-bold text-white">Sign in</Link></section></main>;return children}
