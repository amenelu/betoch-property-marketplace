import {NextRequest,NextResponse} from "next/server";import {authenticated} from "@/lib/api";export async function GET(req:NextRequest){const a=await authenticated(req);if(!a)return NextResponse.json({error:"Authentication required"},{status:401});const q=req.nextUrl.searchParams.get("q")?.trim();let query=a.client.from("profiles").select("id,name,role,agency_name,verification_status,suspended_at,created_at",{count:"exact"}).order("created_at",{ascending:false}).limit(100);if(q)query=query.or(`name.ilike.%${q}%,agency_name.ilike.%${q}%`);const {data,error,count}=await query;return error?NextResponse.json({error:error.message},{status:403}):NextResponse.json({data,count})}
