import {NextRequest,NextResponse} from "next/server";import {authenticated} from "@/lib/api";export async function PATCH(req:NextRequest,{params}:{params:{id:string}}){const a=await authenticated(req);if(!a)return NextResponse.json({error:"Authentication required"},{status:401});const {status,moderation_reason}=await req.json();if(!["published","rejected","archived"].includes(status))return NextResponse.json({error:"Invalid moderation status"},{status:400});if(status==="rejected"&&!String(moderation_reason||"").trim())return NextResponse.json({error:"Rejection reason required"},{status:400});const update={status,moderation_reason:moderation_reason||null,published_at:status==="published"?new Date().toISOString():null};const {data,error}=await a.client.from("properties").update(update).eq("id",params.id).select().single();return error?NextResponse.json({error:error.message},{status:403}):NextResponse.json({data})}
