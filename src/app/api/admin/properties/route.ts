import {NextRequest,NextResponse} from "next/server";import {authenticated} from "@/lib/api";export async function GET(req:NextRequest){const a=await authenticated(req);if(!a)return NextResponse.json({error:"Authentication required"},{status:401});const status=req.nextUrl.searchParams.get("status");let query=a.client.from("properties").select("id,title,slug,status,verification_status,possible_duplicate,created_at,profiles!owner_id(name,role)",{count:"exact"}).order("created_at",{ascending:false}).limit(100);if(status)query=query.eq("status",status);const {data,error,count}=await query;return error?NextResponse.json({error:error.message},{status:403}):NextResponse.json({data,count})}
