import {NextRequest,NextResponse} from "next/server";import {authenticated,bad} from "@/lib/api";
const reasons=["scam","duplicate_listing","incorrect_information","wrong_location","inappropriate_content","property_already_sold_rented","suspected_stolen_photographs","other"];
export async function POST(req:NextRequest){const a=await authenticated(req);if(!a)return NextResponse.json({error:"Authentication required"},{status:401});const body=await req.json();if(!body.property_id||!reasons.includes(body.reason)||String(body.description||"").length>2000)return bad(["Valid property, reason and description are required"]);const {data,error}=await a.client.from("property_reports").insert({property_id:body.property_id,reporter_id:a.user.id,reason:body.reason,description:body.description||null}).select().single();return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({data},{status:201})}
