import {describe,expect,it} from "vitest";import {canAdmin,canModifyProperty,publicCoordinates,validateProperty} from "./validation";
const valid={title:"Bole apartment",property_type:"apartment",listing_type:"sale",price:10000000,area_sqm:100,bedrooms:2,bathrooms:2,latitude:9.01,longitude:38.77};
describe("property validation",()=>{it("accepts valid property creation",()=>expect(validateProperty(valid).success).toBe(true));it.each([["title",""],["property_type","castle"],["listing_type","swap"],["price",-2],["area_sqm",0],["bedrooms",-1],["latitude",91],["longitude",181]])("rejects invalid %s",(key,value)=>expect(validateProperty({...valid,[key]:value}).success).toBe(false));it("rejects excessively long description",()=>expect(validateProperty({...valid,description:"x".repeat(5001)}).success).toBe(false))});
describe("authorization",()=>{it("prevents anonymous edits",()=>expect(canModifyProperty(null,"owner")).toBe(false));it("prevents editing another seller's property",()=>expect(canModifyProperty({id:"attacker",role:"owner"},"owner")).toBe(false));it("allows owner edits and deletion",()=>expect(canModifyProperty({id:"owner",role:"owner"},"owner")).toBe(true));it("allows admin moderation",()=>expect(canModifyProperty({id:"admin",role:"admin"},"owner")).toBe(true));it("never trusts a non-admin role",()=>expect(canAdmin({role:"broker"})).toBe(false));it("requires admin for approvals and verification",()=>expect(canAdmin(null)).toBe(false))});
describe("location privacy",()=>{it("rounds public approximate coordinates",()=>expect(publicCoordinates(9.01555,38.77221,false)).toEqual({latitude:9.02,longitude:38.77}));it("preserves opted-in exact location",()=>expect(publicCoordinates(9.01555,38.77221,true).latitude).toBe(9.01555))});
