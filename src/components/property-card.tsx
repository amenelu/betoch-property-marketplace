"use client";
import Image from "next/image"; import Link from "next/link"; import { useState } from "react";
import type { Property } from "@/lib/types"; import { formatPrice } from "@/lib/data"; import { Bath, BedDouble, Heart, MapPin, Maximize, ShieldCheck } from "./icons";
export function PropertyCard({property}: {property:Property}){ const [saved,setSaved]=useState(false); return <article className="group overflow-hidden rounded-2xl border border-stone-200 bg-white shadow-[0_8px_30px_rgba(31,48,43,.05)] transition hover:-translate-y-1 hover:shadow-[0_16px_40px_rgba(31,48,43,.1)]"><div className="relative aspect-[4/3] overflow-hidden"><Image src={property.image} alt={property.title} fill sizes="(max-width:768px) 100vw, 33vw" className="object-cover transition duration-500 group-hover:scale-[1.03]"/><span className="absolute left-4 top-4 rounded-full bg-white/95 px-3 py-1.5 text-[11px] font-bold uppercase tracking-wider text-[#183c34]">For {property.listingType}</span><button type="button" onClick={()=>setSaved(!saved)} aria-label={saved?"Remove favorite":"Save favorite"} aria-pressed={saved} className="tap-target absolute right-4 top-4 grid place-items-center rounded-full bg-white/95 shadow"><Heart size={19} className={saved?"fill-[#d85d3f] text-[#d85d3f]":"text-stone-600"}/></button></div><Link href={`/properties/${property.slug}`} className="block p-5"><div className="mb-2 flex items-center justify-between gap-2"><p className="text-xl font-bold text-[#173c34]">{formatPrice(property.price)}{property.listingType==="rent"?<span className="text-xs font-medium text-stone-500"> / month</span>:null}</p>{property.verificationStatus!=="unverified"?<span title="Verification checks available"><ShieldCheck size={20} className="shrink-0 text-[#287864]"/></span>:null}</div><h3 className="font-serif text-lg font-semibold text-stone-900 group-hover:text-[#b94730]">{property.title}</h3><p className="mt-1 flex items-center gap-1 text-sm text-stone-500"><MapPin size={14}/>{property.neighborhood}, {property.city}</p><div className="mt-4 flex flex-wrap gap-x-4 gap-y-2 border-t border-stone-100 pt-4 text-xs font-medium text-stone-600">{property.bedrooms>0?<span className="flex items-center gap-1.5"><BedDouble size={16}/>{property.bedrooms} beds</span>:null}<span className="flex items-center gap-1.5"><Bath size={16}/>{property.bathrooms} baths</span><span className="flex items-center gap-1.5"><Maximize size={16}/>{property.areaSqm} m²</span></div></Link></article> }
