"use client";
import Link from "next/link";import {useState} from "react";import {Building2,Clock3,Eye,Heart,Home,LayoutDashboard,MessageCircle,Plus,ShieldCheck,Users} from "./icons";import {properties,formatPrice} from "@/lib/data";import {useDemo} from "./demo-provider";
const sellerLinks=["My listings","Inquiries","Analytics","Profile"] as const;const adminLinks=["Overview","Properties","Users","Verification","Reports"] as const;const icons=[LayoutDashboard,Home,MessageCircle,ShieldCheck,Users];
export function DashboardShell({admin=false}:{admin?:boolean}){const [view,setView]=useState<string>(admin?"Overview":"My listings");const links=admin?adminLinks:sellerLinks;return <div className="min-h-screen bg-[#f6f4ee]"><aside className="fixed inset-y-0 z-20 hidden w-64 bg-[#173c34] p-6 text-white lg:block"><Link href="/" className="flex items-center gap-2 font-serif text-xl font-bold"><Building2/>Betoch</Link><p className="mt-10 text-[10px] font-bold uppercase tracking-[.2em] text-white/40">{admin?"Administration":"Seller workspace"}</p><nav className="mt-4 space-y-1">{links.map((x,i)=>{const Icon=icons[i];return <button onClick={()=>setView(x)} key={x} aria-current={view===x?"page":undefined} className={`flex min-h-11 w-full items-center gap-3 rounded-xl px-3 text-sm ${view===x?"bg-white text-[#173c34]":"text-white/70 hover:bg-white/10"}`}><Icon size={18}/>{x}</button>})}</nav><Link href="/" className="absolute bottom-6 text-sm text-white/60">← Back to marketplace</Link></aside><main className="pb-24 lg:ml-64 lg:pb-0"><header className="flex h-20 items-center justify-between border-b border-stone-200 bg-white px-5 lg:px-10"><div><p className="text-xs text-stone-500">{admin?"Admin workspace":"Seller workspace"}</p><h1 className="font-serif text-xl font-semibold text-[#173c34]">{view}</h1></div>{admin?<Link href="/" className="text-sm font-semibold text-[#173c34]">View marketplace</Link>:<Link href="/dashboard/new" className="flex min-h-11 items-center gap-2 rounded-full bg-[#d85d3f] px-4 text-sm font-bold text-white"><Plus size={16}/>New listing</Link>}</header><div className="p-5 lg:p-10">{admin?<AdminContent view={view}/>:<SellerContent view={view}/>}</div><nav className="fixed inset-x-0 bottom-0 z-30 flex justify-around border-t bg-white p-2 lg:hidden">{links.map((x,i)=>{const Icon=icons[i];return <button onClick={()=>setView(x)} key={x} aria-current={view===x?"page":undefined} className={`grid min-h-14 min-w-14 place-items-center rounded-xl px-2 text-[10px] ${view===x?"bg-[#e7eee9] font-bold text-[#173c34]":"text-stone-500"}`}><span className="grid justify-items-center gap-1"><Icon size={19}/>{x.split(" ")[0]}</span></button>})}</nav></main></div>}
function SellerContent({view}:{view:string}){const {inquiries,updateInquiry,user}=useDemo();if(view==="Inquiries")return <Panel title="Inquiry inbox" intro="Review and update messages from interested buyers and renters.">{inquiries.length?inquiries.map(x=><Row key={x.id} title={x.propertyTitle} subtitle={x.message} badge={x.status}><select value={x.status} onChange={e=>updateInquiry(x.id,e.target.value as typeof x.status)} className="rounded-lg border px-3 py-2 text-xs"><option>new</option><option>read</option><option>responded</option><option>closed</option></select></Row>):<Empty title="No inquiries yet" text="New property inquiries will appear here."/>}</Panel>;if(view==="Analytics")return <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={Eye} label="Total views" value="2,418" change="+12% this month"/><Stat icon={Heart} label="Favorites" value="86" change="+8 this week"/><Stat icon={MessageCircle} label="Inquiries" value={String(inquiries.length+34)} change="5 need a reply"/><Stat icon={Home} label="Active listings" value="6" change="2 pending review"/></div><Panel title="Performance by property" intro="Last 30 days"><SimpleTable headers={["Property","Views","Favorites","Inquiries","Phone","WhatsApp"]} rows={properties.slice(0,4).map((p,i)=>[p.title,String(421-i*73),String(18-i*3),String(9-i),String(7-i),String(11-i)])}/></Panel></>;if(view==="Profile")return <Panel title="Seller profile" intro="Control the information buyers see."><form className="grid max-w-2xl gap-5 sm:grid-cols-2" onSubmit={e=>{e.preventDefault();alert("Profile saved")}}><Field label="Display name" defaultValue={user?.name||"Mekdes Tesfaye"}/><Field label="Agency" defaultValue="Abay Homes"/><Field label="Phone" defaultValue="+251 911 234 567"/><Field label="WhatsApp" defaultValue="+251 911 234 567"/><label className="sm:col-span-2 text-sm font-semibold">Bio<textarea className="mt-2 h-28 w-full rounded-xl border p-3 font-normal" defaultValue="Residential property broker serving Addis Ababa."/></label><label className="flex items-center gap-2 text-sm"><input type="checkbox" defaultChecked/>Show phone publicly</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" defaultChecked/>Show WhatsApp publicly</label><button className="min-h-11 rounded-full bg-[#173c34] px-5 font-bold text-white sm:col-span-2 sm:w-fit">Save profile</button></form></Panel>;return <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={Eye} label="Total views" value="2,418" change="+12% this month"/><Stat icon={Heart} label="Favorites" value="86" change="+8 this week"/><Stat icon={MessageCircle} label="Inquiries" value={String(inquiries.length+34)} change="5 need a reply"/><Stat icon={Home} label="Active listings" value="6" change="2 pending review"/></div><Panel title="Your properties" action={<Link href="/dashboard/new" className="font-bold text-[#d85d3f]">Add property</Link>}><SimpleTable headers={["Property","Price","Status","Views","Action"]} rows={properties.slice(0,4).map((p,i)=>[p.title,formatPrice(p.price),i===2?"Pending review":"Published",String(421-i*73),"Edit"])} linkLast/></Panel></>}
function AdminContent({view}:{view:string}){const {reports,updateReport}=useDemo();const [listingStatus,setListingStatus]=useState<Record<string,string>>({});const [usersState,setUsersState]=useState<Record<string,string>>({});const [checks,setChecks]=useState<Record<string,string>>({});if(view==="Properties")return <Panel title="Property moderation" intro="Review submitted listings and publication status.">{properties.map((p,i)=><Row key={p.id} title={p.title} subtitle={`${p.neighborhood} · ${formatPrice(p.price)}`} badge={listingStatus[p.id]|| (i<2?"pending review":"published")}><div className="flex gap-2"><button onClick={()=>setListingStatus(s=>({...s,[p.id]:"published"}))} className="rounded-lg bg-emerald-100 px-3 py-2 text-xs font-bold text-emerald-800">Approve</button><button onClick={()=>setListingStatus(s=>({...s,[p.id]:"rejected"}))} className="rounded-lg bg-red-100 px-3 py-2 text-xs font-bold text-red-800">Reject</button></div></Row>)}</Panel>;if(view==="Users")return <Panel title="User management" intro="Search, review and suspend marketplace accounts.">{["Mekdes Tesfaye · Broker","Daniel Bekele · Owner","Abel Girma · Buyer","Selamawit Girma · Broker"].map((x,i)=><Row key={x} title={x} subtitle={`user${i+1}@example.et`} badge={usersState[x]||"active"}><button onClick={()=>setUsersState(s=>({...s,[x]:s[x]==="suspended"?"active":"suspended"}))} className="rounded-lg border px-3 py-2 text-xs font-bold">{usersState[x]==="suspended"?"Reactivate":"Suspend"}</button></Row>)}</Panel>;if(view==="Verification")return <Panel title="Manual verification queue" intro="Each check is recorded separately. Approval is not a legal ownership guarantee.">{["Seller identity · Mekdes Tesfaye","Location · Sunlit three-bedroom apartment","Ownership document · Private garden villa","Building document · New-build condominium"].map(x=><Row key={x} title={x} subtitle="Submitted for manual admin review" badge={checks[x]||"pending"}><div className="flex gap-2"><button onClick={()=>setChecks(s=>({...s,[x]:"approved"}))} className="rounded-lg bg-emerald-100 px-3 py-2 text-xs font-bold text-emerald-800">Approve</button><button onClick={()=>setChecks(s=>({...s,[x]:"rejected"}))} className="rounded-lg bg-red-100 px-3 py-2 text-xs font-bold text-red-800">Reject</button></div></Row>)}</Panel>;if(view==="Reports")return <Panel title="Listing reports" intro="Investigate user reports and record a resolution.">{reports.length?reports.map(x=><Row key={x.id} title={x.propertyTitle} subtitle={`${x.reason.replaceAll("_"," ")} · ${x.description}`} badge={x.status}><select value={x.status} onChange={e=>updateReport(x.id,e.target.value as typeof x.status)} className="rounded-lg border px-3 py-2 text-xs"><option>open</option><option>reviewing</option><option>resolved</option><option>dismissed</option></select></Row>):<Empty title="No open reports" text="Reports submitted from property pages will appear here."/>}</Panel>;return <><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Stat icon={Users} label="Total users" value="1,284" change="+42 this month"/><Stat icon={Home} label="Published" value="486" change="38 pending review"/><Stat icon={ShieldCheck} label="Verified" value="214" change="12 awaiting checks"/><Stat icon={MessageCircle} label="Open reports" value={String(reports.filter(x=>x.status==="open").length)} change="Requires review"/></div><div className="mt-8 grid gap-6 xl:grid-cols-2"><Panel title="Listings awaiting review">{properties.slice(0,3).map(p=><Row key={p.id} title={p.title} subtitle={p.neighborhood} badge="pending"/>)}</Panel><Panel title="Verification queue">{["Ownership document · #BTH-1048","Location check · #BTH-1032","Identity document · #USR-488"].map(x=><Row key={x} title={x} subtitle="Awaiting admin" badge="pending"/>)}</Panel></div></>}
function Stat({icon:Icon,label,value,change}:{icon:typeof Eye;label:string;value:string;change:string}){return <div className="rounded-2xl border bg-white p-5"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#e7eee9] text-[#287864]"><Icon size={19}/></span><p className="mt-5 text-sm text-stone-500">{label}</p><p className="mt-1 text-3xl font-bold text-[#173c34]">{value}</p><p className="mt-2 text-xs text-stone-500">{change}</p></div>}
function Panel({title,intro,action,children}:{title:string;intro?:string;action?:React.ReactNode;children:React.ReactNode}){return <section className="mt-8 overflow-hidden rounded-2xl border bg-white"><div className="flex items-center justify-between border-b p-5"><div><h2 className="font-serif text-xl font-semibold text-[#173c34]">{title}</h2>{intro?<p className="mt-1 text-xs text-stone-500">{intro}</p>:null}</div>{action}</div><div className="p-5">{children}</div></section>}
function Row({title,subtitle,badge,children}:{title:string;subtitle:string;badge:string;children?:React.ReactNode}){return <div className="flex flex-col gap-3 border-b py-4 last:border-0 sm:flex-row sm:items-center"><div className="min-w-0 flex-1"><p className="font-semibold">{title}</p><p className="truncate text-xs text-stone-500">{subtitle}</p></div><span className="w-fit rounded-full bg-stone-100 px-2.5 py-1 text-[11px] font-bold capitalize">{badge}</span>{children}</div>}
function SimpleTable({headers,rows,linkLast=false}:{headers:string[];rows:string[][];linkLast?:boolean}){return <div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead><tr>{headers.map(x=><th className="border-b px-3 py-3 text-xs uppercase text-stone-500" key={x}>{x}</th>)}</tr></thead><tbody>{rows.map((row,i)=><tr key={i}>{row.map((x,j)=><td className="border-b px-3 py-4" key={j}>{linkLast&&j===row.length-1?<Link href={`/dashboard/properties/${properties[i].id}/edit`} className="font-bold text-[#d85d3f]">{x}</Link>:x}</td>)}</tr>)}</tbody></table></div>}
function Empty({title,text}:{title:string;text:string}){return <div className="py-12 text-center"><Clock3 className="mx-auto text-stone-400"/><p className="mt-3 font-serif text-xl text-[#173c34]">{title}</p><p className="mt-1 text-sm text-stone-500">{text}</p></div>}
function Field({label,defaultValue}:{label:string;defaultValue:string}){return <label className="text-sm font-semibold">{label}<input defaultValue={defaultValue} className="mt-2 h-12 w-full rounded-xl border px-3 font-normal"/></label>}
